# node_modules klasörü (gereksiz büyük ve her zaman local'de kurulmalı)
node_modules/

# Ortam değişkenleri (şifreler, bağlantı stringleri vs.)
.env

# Firebase servis hesabı anahtarı (çok kritik!)
src/config/serviceAccountKey.json

# Firebase config dosyası (gizli bilgileri taşıyorsa)
src/config/firebase.js

# Log dosyaları
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE / OS dosyaları
.DS_Store
*.swp

# PostgreSQL dump veya geçici db dosyaları (eğer varsa)
*.sqlite
*.log
*.dump
